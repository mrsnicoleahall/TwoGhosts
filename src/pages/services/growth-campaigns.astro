---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.astro';
import Footer from '../../components/Footer.astro';
---

<Layout title="Growth Campaigns - Two Ghosts Strategy">
  <Navigation />

  ---
  import { getServiceBySlug } from '../../config/services';
  const slug = Astro.url.pathname.replace('/services/', '').replace('/', '');
  const meta = getServiceBySlug(slug);
  ---

  <section class="pt-32 pb-16">
    <div class="w-full px-6 sm:px-10 lg:px-16 xl:px-24 2xl:px-32 grid gap-12 lg:grid-cols-[auto,minmax(0,1fr)] items-start">
      <div class="flex items-center justify-center">
        <div class="w-32 h-32 sm:w-40 sm:h-40 rounded-3xl bg-black/70 border border-white/15 shadow-lg flex items-center justify-center">
          {meta && <img src={meta.icon} alt={meta.name} class="w-24 h-24 sm:w-28 sm:h-28" />}
        </div>
      </div>

      <div>
        <p class="text-sm tracking-[0.25em] uppercase text-gray-400 font-['Raleway']">
          Services
        </p>
        <h1 class="mt-3 text-4xl sm:text-5xl font-black text-white font-['Raleway']">
          {meta ? meta.name : 'Service'}
        </h1>
        
        <div class="section-heading relative mb-8">
          <div class="bgtext-section">Results That Matter</div>
        </div>

        <div class="mt-16 space-y-16">
      <!-- Section 1 -->
      <div>
        <h2 class="text-4xl font-bold text-white mb-6">
          Campaigns That Move the Needle
        </h2>
        <div class="space-y-4 text-xl text-gray-50">
          <p>
            Campaigns fail for two reasons: bad strategy or bad execution. Usually both.
          </p>
          <p>
            Most campaigns are built backwards—starting with tactics instead of outcomes, channels instead of audiences, creative instead of message. They look good in a deck but fall apart in market.
          </p>
          <p>
            We build campaigns that work because we start with what matters: a clear goal, a specific audience, and a message that actually resonates. Then we build the tactics, creative, and channel mix to make it happen.
          </p>
        </div>
      </div>

      <!-- Section 2 -->
      <div>
        <h2 class="text-4xl font-bold text-white mb-6">
          What We Do
        </h2>
        <p class="text-xl text-gray-50 mb-6">
          We design and execute integrated campaigns that drive measurable results—whether that's leads, revenue, awareness, or all three.
        </p>
        <ul class="list-disc list-inside space-y-3 text-xl text-gray-50 ml-6">
          <li>Campaign strategy and planning</li>
          <li>Multi-channel campaign design and execution</li>
          <li>Content development and creative direction</li>
          <li>Paid media strategy and optimization</li>
          <li>Email marketing and nurture campaigns</li>
          <li>Landing page and conversion optimization</li>
          <li>Campaign measurement and optimization</li>
        </ul>
      </div>

      <!-- Section 3 -->
      <div>
        <h2 class="text-4xl font-bold text-white mb-6">
          Who This Is For
        </h2>
        <div class="space-y-4 text-xl text-gray-50">
          <p>
            Companies launching something new. Teams who need to hit aggressive growth targets. Leaders tired of campaigns that generate buzz but not business.
          </p>
          <p>
            If you're ready for campaigns that actually work—let's build them.
          </p>
        </div>
      </div>

      <!-- CTA -->
      <div class="text-center mt-12">
        <a
          href="/schedule"
          class="btn-gradient inline-block px-8 py-3 text-white font-black rounded-full"
        >
          Let's Talk Strategy
        </a>
      </div>
    </div>
  </section>

  <Footer />
</Layout>
